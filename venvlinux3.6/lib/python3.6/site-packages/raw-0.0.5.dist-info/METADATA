Metadata-Version: 2.1
Name: raw
Version: 0.0.5
Summary: Ruqqus API Wrapper
Home-page: https://github.com/ithinkimokay/raw
Author: diogenesjunior
Author-email: diogenesjunior@protonmail.com
License: UNKNOWN
Keywords: Ruqqus API RSAW
Platform: UNKNOWN
Requires-Dist: requests

RAW
===

**Ruqqus API Wrapper**

RAW is a very light API wrapper, made for `Ruqqus <https://ruqqus.com>`_. It allows easier access to Ruqqus' API and its endpoints, returning a raw JSON result.

Installation
------------

.. code-block:: bash

   pip3 install raw

How to use
----------

Sending a get request:

.. code-block:: python

    from raw import Ruqqus

    ruqqus = Ruqqus(client_id=client_id,
                    client_secret=client_secret,
                    user_agent=user_agent,
                    access_token=access_token,
                    refresh_token=refresh_token,
                    token_file='/tmp/token')
    comment = ruqqus.get('/comment/{c_id}')
    print(comment)

Sending a post request:

.. code-block:: python

    from raw import Ruqqus

    ruqqus = Ruqqus(client_id=client_id,
                    client_secret=client_secret,
                    user_agent=user_agent,
                    access_token=access_token,
                    refresh_token=refresh_token,
                    token_file='/tmp/token')
    title = '' #title here
    url = '' #url here
    parameters = {'board': 'general',
                  'title': title,
                  'url' : url}
    submit = ruqqus.post('/api/v1/submit', data=parameters)
    print(submit)


